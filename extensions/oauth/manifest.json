{
  "name": "oauth",
  "version": "0.1.0",
  "description": "OAuth connector extension — declares credential permissions for Google, GitHub, and other external services; core user sign-in is configured separately",
  "type": "extension",
  "status": "experimental",
  "required_permissions": ["credential_access"],
  "external_connections": ["accounts.google.com", "github.com"],
  "dangerous_operations": false,
  "approval_required_operations": ["credential_update"],
  "author": "Zero-Employee Orchestrator Team"
}
